import React, { PureComponent } from 'react';

class Auth extends PureComponent {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>Login will render here...</div>
        );
    }
}

export default Auth;